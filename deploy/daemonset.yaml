apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: kssd-driver
  namespace: kube-system
  labels:
    app.kubernetes.io/name: kssd-driver
    app.kubernetes.io/component: slm-driver
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: kssd-driver
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kssd-driver
    spec:
      serviceAccountName: kssd-driver
      tolerations:
      - operator: Exists
      containers:
      - name: kssd-driver
        image: kssd-driver:latest
        imagePullPolicy: IfNotPresent
        args:
        - kubelet-plugin
        - --node-name=$(NODE_NAME)
        - --v=3
        env:
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        volumeMounts:
        - name: socket-dir
          mountPath: /var/lib/kubelet/plugins/kubectl-server-side-drain
        - name: registration-dir
          mountPath: /var/lib/kubelet/plugins_registry
        securityContext:
          privileged: true
          runAsUser: 0
      volumes:
      - name: socket-dir
        hostPath:
          path: /var/lib/kubelet/plugins/kubectl-server-side-drain
          type: DirectoryOrCreate
      - name: registration-dir
        hostPath:
          path: /var/lib/kubelet/plugins_registry
          type: DirectoryOrCreate
